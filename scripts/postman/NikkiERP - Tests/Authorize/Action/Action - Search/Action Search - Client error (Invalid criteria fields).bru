meta {
  name: Action Search - Client error (Invalid criteria fields)
  type: http
  seq: 5
}

get {
  url: {{api_host}}/v1/authorize/actions?page=0&size=4&graph={"if":["invalid_field", "=", "invalid_value"]}
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 4
  graph: {"if":["invalid_field", "=", "invalid_value"]}
}

settings {
  encodeUrl: true
}
