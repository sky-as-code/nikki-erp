meta {
  name: Login - Authenticate
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/authn/login
  body: json
  auth: inherit
}

body:json {
  {
      "attemptId": "{{attemptId}}",
      "passwords": {
        "password": "Passwo0rd123"
      }
  }
}

vars:pre-request {
  email: owj@nikki.com
}

script:post-response {
  const payload = res.getBody();
  bru.setEnvVar('accessToken', payload?.data?.accessToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
