meta {
  name: Resource Get - Columns - Success
  type: http
  seq: 2
}

get {
  url: {{api_host}}/v1/authorize/resources/{{resource_id}}?columns=name&columns=code&columns=max_scope
  body: none
  auth: inherit
}

params:query {
  columns: name
  columns: code
  columns: max_scope
}

script:post-response {
  const { testHttpResponse } = require('./scripts/common-test-response');
  
  const schema = {
    type: 'object',
    required: ['name', 'code', 'max_scope'],
    properties: {
      id: {
        type: 'string',
        minLength: 1,
      },
      name: {
        type: 'string',
        minLength: 1,
      },
      code: {
        type: 'string',
        minLength: 1,
      },
      max_scope: {
        type: 'string',
        minLength: 1,
      },
    },
    additionalProperties: false,
  };
  
  testHttpResponse(schema, 200);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
