meta {
  name: Action Create - Missing required fields - Validation errors
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/authorize/actions
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields('name', 'code', 'resource_id');
  
}

settings {
  encodeUrl: true
  timeout: 0
}
