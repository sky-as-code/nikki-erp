meta {
  name: Action Update - Client error (No match etag)
  type: http
  seq: 3
}

put {
  url: {{api_host}}/v1/authorize/actions/{{action_id}}
  body: json
  auth: inherit
}

body:json {
  {
      "description": "Description",
      "etag": "1752032294635863001"
  }
}

script:post-response {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
  test("Returns validation_error code", function () {
      const json = res.getBody();
      expect(json.code).to.eql("validation_error");
  });
  
  test("Returns etag conflict message", function () {
      const json = res.getBody();
      expect(json.details).to.have.property("etag", "etag mismatched");
  });
  
}

settings {
  encodeUrl: true
}
