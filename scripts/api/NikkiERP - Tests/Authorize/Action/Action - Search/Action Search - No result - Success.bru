meta {
  name: Action Search - No result - Success
  type: http
  seq: 4
}

get {
  url: {{api_host}}/v1/authorize/resources/{{resource_id}}/actions?page=99
  body: none
  auth: inherit
}

params:query {
  page: 99
}

script:post-response {
  const pkg = require('./scripts/search-success');
  
  pkg.testSearch(50, 99, 0);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
