meta {
  name: Role Create - Missing required fields - Validation errors
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/authorize/roles
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields('name', 'is_requestable', 'is_required_attachment', 'is_required_comment');
  
}

settings {
  encodeUrl: true
  timeout: 0
}
