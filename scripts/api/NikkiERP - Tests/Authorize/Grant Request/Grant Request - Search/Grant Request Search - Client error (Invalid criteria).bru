meta {
  name: Grant Request Search - Client error (Invalid criteria)
  type: http
  seq: 4
}

get {
  url: {{api_host}}/v1/authorize/grant-requests?page=0&size=4&graph={"if":["invalid_field", "=", "invalid_value"]}
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 4
  graph: {"if":["invalid_field", "=", "invalid_value"]}
}

settings {
  encodeUrl: true
}
