meta {
  name: Org unit Create - Missing required fields - Validation errors
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/identity/orgunits
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields('name', 'org_id');
  
}

settings {
  encodeUrl: true
  timeout: 0
}
