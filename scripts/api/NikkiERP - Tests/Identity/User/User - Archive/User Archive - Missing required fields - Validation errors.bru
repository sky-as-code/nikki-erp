meta {
  name: User Archive - Missing required fields - Validation errors
  type: http
  seq: 5
}

post {
  url: {{api_host}}/v1/identity/users/{{user_id}}/archived
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields("etag", "is_archived");
  
}

settings {
  encodeUrl: true
  timeout: 0
}
