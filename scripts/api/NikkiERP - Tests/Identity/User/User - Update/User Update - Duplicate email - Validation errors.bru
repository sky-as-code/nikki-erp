meta {
  name: User Update - Duplicate email - Validation errors
  type: http
  seq: 8
}

put {
  url: {{api_host}}/v1/identity/users/{{user_id}}
  body: json
  auth: inherit
}

body:json {
  {
      "email": "{{email}}",
      "etag": "{{etag}}"
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-duplicate-value');
  
  pkg.testDuplicateValues(['email']);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
