meta {
  name: User Search - Order by root field - Success
  type: http
  seq: 11
}

get {
  url: {{api_host}}/v1/identity/users?graph={"order": [["display_name", "desc"]]}
  body: none
  auth: inherit
}

params:query {
  graph: {"order": [["display_name", "desc"]]}
}

script:post-response {
  const pkg = require('./scripts/search-success');
  
  pkg.testSearch(50);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
