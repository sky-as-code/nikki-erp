meta {
  name: User Search - Create test data - Loop 100
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/identity/users
  body: json
  auth: inherit
}

body:json {
  {
      "displayName": "Tester {{$randomFirstName}} {{loop_count}}",
      "email": "{{loop_count}}-{{$randomEmail}}"
  }
}

script:pre-request {
  let body = req.getBody();
  console.log('Creating test user: ', bru.interpolate(body.displayName));
}

script:post-response {
  test("Status code is 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
  
  let i = bru.getVar("loop_count");
  
  if (i > 0) {
      bru.setVar("loop_count", --i);
      console.log("To loop: ", i)
      bru.runner.setNextRequest('User Search - Create test data - Loop 100');
  } else {
      console.log("To test case")
      bru.deleteVar("loop_count")
      bru.runner.setNextRequest('User Search - No criteria, No paging - Success');
  }
  
}

settings {
  encodeUrl: true
}
