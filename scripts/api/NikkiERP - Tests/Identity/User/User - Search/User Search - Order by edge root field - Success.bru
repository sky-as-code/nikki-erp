meta {
  name: User Search - Order by edge root field - Success
  type: http
  seq: 12
}

get {
  url: {{api_host}}/v1/identity/users?graph={"order": [["groups.name", "desc"]]}
  body: none
  auth: inherit
}

params:query {
  graph: {"order": [["groups.name", "desc"]]}
}

settings {
  encodeUrl: true
  timeout: 0
}
