meta {
  name: User Search - Order by edge JSON subfield - Success
  type: http
  seq: 13
}

get {
  url: {{api_host}}/v1/identity/users?graph={"order": [["user_status.label.vi_VN", "desc"]]}
  body: none
  auth: inherit
}

params:query {
  graph: {"order": [["user_status.label.vi_VN", "desc"]]}
}

script:post-response {
  const pkg = require('./scripts/search-success');
  
  pkg.testSearch(50);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
