meta {
  name: User Get - Non-exist column - Validation error
  type: http
  seq: 5
}

get {
  url: {{api_host}}/v1/identity/users/{{user_id}}?columns=display_name&columns=email&columns=bla_bla_field
  body: none
  auth: inherit
}

params:query {
  columns: display_name
  columns: email
  columns: bla_bla_field
}

script:post-response {
  const pkg = require('./scripts/error-400-nonexist-fields');
  
  pkg.testNonExistFields('bla_bla_field');
  
}

settings {
  encodeUrl: true
  timeout: 0
}
