meta {
  name: Group Create - Missing required fields - Validation errors
  type: http
  seq: 3
}

post {
  url: {{api_host}}/v1/identity/groups
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields("name", "owner_id");
  
}

settings {
  encodeUrl: true
  timeout: 0
}
