meta {
  name: Org Update - Unmatched etag - Validation errors
  type: http
  seq: 2
}

put {
  url: {{api_host}}/v1/identity/organizations/{{org_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "display_name": "{{$randomCompanyName}} Updated",
    "etag": "old-etag"
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-etag-unmatched');
  
  pkg.testUnmatchedEtag();
  
}

settings {
  encodeUrl: true
  timeout: 0
}
