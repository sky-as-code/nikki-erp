meta {
  name: Party Update - Missing required fields - Validation errors
  type: http
  seq: 4
}

put {
  url: {{api_host}}/v1/contacts/parties/{{party_id}}
  body: json
  auth: inherit
}

body:json {
  {
  }
}

settings {
  encodeUrl: true
}
