meta {
  name: Party Create - Missing required fields - Validation errors
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/contacts/parties
  body: json
  auth: inherit
}

body:json {
  {
  }
}

settings {
  encodeUrl: true
}
