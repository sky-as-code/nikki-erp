meta {
  name: CommChannel Create - Missing required fields - Validation errors
  type: http
  seq: 2
}

post {
  url: {{api_host}}/v1/contacts/parties/channels
  body: json
  auth: inherit
}

body:json {
  {}
}

settings {
  encodeUrl: true
}
