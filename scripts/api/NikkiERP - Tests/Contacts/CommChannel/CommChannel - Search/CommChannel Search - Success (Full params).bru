meta {
  name: CommChannel Search - Success (Full params)
  type: http
  seq: 1
}

get {
  url: {{api_host}}/v1/contacts/parties/channels?page=0&size=2&graph={"if":["type", "=", "post"]}
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 2
  graph: {"if":["type", "=", "post"]}
}

settings {
  encodeUrl: true
}
