meta {
  name: CommChannel Update - Success
  type: http
  seq: 1
}

put {
  url: {{api_host}}/v1/contacts/parties/channels/{{channel_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "partyId": "{{party_id}}",
    "type": "post",
    "valueJson" : {
      "numberStreet": "297 An Duong Vuong",
      "ward": "phuong 3"
    },
    "etag": "{{etag}}"
  }
}

settings {
  encodeUrl: true
}
