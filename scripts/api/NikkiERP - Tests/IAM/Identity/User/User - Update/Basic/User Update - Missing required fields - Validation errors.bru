meta {
  name: User Update - Missing required fields - Validation errors
  type: http
  seq: 3
}

patch {
  url: {{api_host}}/v1/iam/iam_user/{{user_id}}
  body: json
  auth: inherit
}

body:json {
  {
  }
}

script:post-response {
  const pkg = require('./scripts/error-400-fields-missing');
  
  pkg.testMissingFields("etag");
  
}

settings {
  encodeUrl: true
  timeout: 0
}
