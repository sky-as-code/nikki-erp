meta {
  name: Role Create - Malform payload - Validation errors
  type: http
  seq: 3
}

post {
  url: {{api_host}}/v1/iam/iam_role
  body: json
  auth: inherit
}

body:json {
  {
    "name": "",
    "is_requestable": "bad_type",
  
}

script:post-response {
  const pkg = require('./scripts/error-400-malform-payload');
  
  pkg.testMalformPayload();
  
}

settings {
  encodeUrl: true
  timeout: 0
}
