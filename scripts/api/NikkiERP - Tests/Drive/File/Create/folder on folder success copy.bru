meta {
  name: folder on folder success copy
  type: http
  seq: 11
}

post {
  url: {{api_host}}/v1/drive/files
  body: multipartForm
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:multipart-form {
  name: child-11
  isFolder: true
  visibility: owner
  file: 
  parentDriveFileRef: {{parentFileId}}
}

vars:pre-request {
  parentFileId: 01KK79ER9XXFEM545W0ZBQ4JCF
}

tests {
  test("Status is 201", function() {
      expect(res.getStatus()).to.equal(201);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
