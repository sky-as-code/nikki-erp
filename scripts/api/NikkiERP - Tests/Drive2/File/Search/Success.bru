meta {
  name: Success
  type: http
  seq: 1
}

get {
  url: {{api_host}}/v1/drive2/files?page=0&size=20
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
