meta {
  name: Model Metadata - Exists
  type: http
  seq: 4
}

post {
  url: {{api_host}}/v1/essential/model-metadata/exists
  body: json
  auth: inherit
}

body:json {
  {
    "ids": [
      "{{model_metadata_id}}"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

