meta {
  name: Module - Exists
  type: http
  seq: 4
}

post {
  url: {{api_host}}/v1/essential/modules/exists
  body: json
  auth: inherit
}

body:json {
  {
    "ids": [
      "{{module_id}}"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

