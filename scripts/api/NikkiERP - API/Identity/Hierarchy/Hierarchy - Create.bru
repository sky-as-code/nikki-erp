meta {
  name: Hierarchy - Create
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/identity/hierarchy
  body: json
  auth: inherit
}

body:json {
  {
      "name": "Giam Doc Cong ty Co Phan Core Vision",
      "orgID": "{{org_id}}"
  }
}

script:post-response {
  const { id, etag } = res.getBody();
  id && bru.setVar('hierarchy_id', id);
  etag && bru.setVar('etag', etag);
  
}

settings {
  encodeUrl: true
}
