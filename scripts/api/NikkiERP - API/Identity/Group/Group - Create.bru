meta {
  name: Group - Create
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/identity/groups
  body: json
  auth: inherit
}

body:json {
  {
      "name": "{{$randomJobArea}} Team",
      "description": "This is {{$randomJobArea}} team"
  }
}

script:post-response {
  const { id, etag } = res.getBody();
  id && bru.setVar('group_id', id);
  etag && bru.setVar('etag', etag);
  
}

settings {
  encodeUrl: true
}
