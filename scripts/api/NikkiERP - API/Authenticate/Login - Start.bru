meta {
  name: Login - Start
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/authn/login/start
  body: json
  auth: inherit
}

body:json {
  {
      "username": "owner@nikki.com"
  }
}

vars:pre-request {
  email: jaycee_hermiston@yahoo.com
}

script:post-response {
  const payload = res.getBody();
  const { attempt_id } = payload;
  attempt_id && bru.setVar('attempt_id', attempt_id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
