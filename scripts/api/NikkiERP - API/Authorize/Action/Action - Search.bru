meta {
  name: Action - Search
  type: http
  seq: 5
}

get {
  url: {{api_host}}/v1/authorize/actions?page=0&size=4&graph={"if":["name", "=", "Create"]}
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 4
  graph: {"if":["name", "=", "Create"]}
}

settings {
  encodeUrl: true
}
