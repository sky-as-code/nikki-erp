meta {
  name: Attribute value - Get By Id
  type: http
  seq: 3
}

get {
  url: {{api_host}}/v1/{{org_id}}/inventory/products/{{product_id}}/attributes/{{attribute_id}}/values/{{attribute_value_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
