meta {
  name: Attribute Value - Create
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/{{org_id}}/inventory/products/{{product_id}}/attributes/{{attribute_id}}/values
  body: json
  auth: inherit
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
