meta {
  name: Variant - Create
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/{{org_id}}/inventory/products/{{product_id}}/variants
  body: json
  auth: inherit
}

body:json {
  {
    "name": {
      "vi" : "Áo thun màu đỏ M",
      "en" : "Red T-shirt size M"
    },
    "sku": "TSHIRT-RED-M",
    "barcode": "8938501234567",
    "proposedPrice": 199000,
    "attributes": {
      "color": {
        "vi_VN": "Đỏ",
        "en_US": "Red"
      },
      "size": 19
    }
  }
}

vars:pre-request {
  org_id: 01JWNY20G23KD4RV5VWYABQYHD
}

script:post-response {
  const { id, etag } = res.getBody();
  id && bru.setVar('variant_id', id);
  etag && bru.setVar('etag', etag);
}

settings {
  encodeUrl: true
  timeout: 0
}
