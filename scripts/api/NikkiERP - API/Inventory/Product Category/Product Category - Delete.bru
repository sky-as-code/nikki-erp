meta {
  name: Product Category - Delete
  type: http
  seq: 5
}

delete {
  url: {{api_host}}/v1/{{org_id}}/inventory/products-category/{{product_category_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
