meta {
  name: Attribute Group - Create
  type: http
  seq: 1
}

post {
  url: {{api_host}}/v1/{{org_id}}/inventory/products/{{product_id}}/attribute-group
  body: json
  auth: inherit
}

body:json {
  {
    "name": {
       "en": "Machinery parts",
       "vi": "Bộ phận máy móc"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
