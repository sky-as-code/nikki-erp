meta {
  name: Attribute Group - Delete
  type: http
  seq: 5
}

delete {
  url: {{api_host}}/v1/{{org_id}}/inventory/products/{{product_id}}/attribute-group/{{attribute_group_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
