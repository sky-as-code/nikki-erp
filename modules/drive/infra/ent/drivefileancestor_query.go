// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefile"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefileancestor"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/predicate"
)

// DriveFileAncestorQuery is the builder for querying DriveFileAncestor entities.
type DriveFileAncestorQuery struct {
	config
	ctx           *QueryContext
	order         []drivefileancestor.OrderOption
	inters        []Interceptor
	predicates    []predicate.DriveFileAncestor
	withDriveFile *DriveFileQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DriveFileAncestorQuery builder.
func (dfaq *DriveFileAncestorQuery) Where(ps ...predicate.DriveFileAncestor) *DriveFileAncestorQuery {
	dfaq.predicates = append(dfaq.predicates, ps...)
	return dfaq
}

// Limit the number of records to be returned by this query.
func (dfaq *DriveFileAncestorQuery) Limit(limit int) *DriveFileAncestorQuery {
	dfaq.ctx.Limit = &limit
	return dfaq
}

// Offset to start from.
func (dfaq *DriveFileAncestorQuery) Offset(offset int) *DriveFileAncestorQuery {
	dfaq.ctx.Offset = &offset
	return dfaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dfaq *DriveFileAncestorQuery) Unique(unique bool) *DriveFileAncestorQuery {
	dfaq.ctx.Unique = &unique
	return dfaq
}

// Order specifies how the records should be ordered.
func (dfaq *DriveFileAncestorQuery) Order(o ...drivefileancestor.OrderOption) *DriveFileAncestorQuery {
	dfaq.order = append(dfaq.order, o...)
	return dfaq
}

// QueryDriveFile chains the current query on the "drive_file" edge.
func (dfaq *DriveFileAncestorQuery) QueryDriveFile() *DriveFileQuery {
	query := (&DriveFileClient{config: dfaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dfaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dfaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(drivefileancestor.Table, drivefileancestor.FieldID, selector),
			sqlgraph.To(drivefile.Table, drivefile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, drivefileancestor.DriveFileTable, drivefileancestor.DriveFileColumn),
		)
		fromU = sqlgraph.SetNeighbors(dfaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DriveFileAncestor entity from the query.
// Returns a *NotFoundError when no DriveFileAncestor was found.
func (dfaq *DriveFileAncestorQuery) First(ctx context.Context) (*DriveFileAncestor, error) {
	nodes, err := dfaq.Limit(1).All(setContextOp(ctx, dfaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{drivefileancestor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) FirstX(ctx context.Context) *DriveFileAncestor {
	node, err := dfaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DriveFileAncestor ID from the query.
// Returns a *NotFoundError when no DriveFileAncestor ID was found.
func (dfaq *DriveFileAncestorQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = dfaq.Limit(1).IDs(setContextOp(ctx, dfaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{drivefileancestor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) FirstIDX(ctx context.Context) string {
	id, err := dfaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DriveFileAncestor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DriveFileAncestor entity is found.
// Returns a *NotFoundError when no DriveFileAncestor entities are found.
func (dfaq *DriveFileAncestorQuery) Only(ctx context.Context) (*DriveFileAncestor, error) {
	nodes, err := dfaq.Limit(2).All(setContextOp(ctx, dfaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{drivefileancestor.Label}
	default:
		return nil, &NotSingularError{drivefileancestor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) OnlyX(ctx context.Context) *DriveFileAncestor {
	node, err := dfaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DriveFileAncestor ID in the query.
// Returns a *NotSingularError when more than one DriveFileAncestor ID is found.
// Returns a *NotFoundError when no entities are found.
func (dfaq *DriveFileAncestorQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = dfaq.Limit(2).IDs(setContextOp(ctx, dfaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{drivefileancestor.Label}
	default:
		err = &NotSingularError{drivefileancestor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) OnlyIDX(ctx context.Context) string {
	id, err := dfaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DriveFileAncestors.
func (dfaq *DriveFileAncestorQuery) All(ctx context.Context) ([]*DriveFileAncestor, error) {
	ctx = setContextOp(ctx, dfaq.ctx, ent.OpQueryAll)
	if err := dfaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DriveFileAncestor, *DriveFileAncestorQuery]()
	return withInterceptors[[]*DriveFileAncestor](ctx, dfaq, qr, dfaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) AllX(ctx context.Context) []*DriveFileAncestor {
	nodes, err := dfaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DriveFileAncestor IDs.
func (dfaq *DriveFileAncestorQuery) IDs(ctx context.Context) (ids []string, err error) {
	if dfaq.ctx.Unique == nil && dfaq.path != nil {
		dfaq.Unique(true)
	}
	ctx = setContextOp(ctx, dfaq.ctx, ent.OpQueryIDs)
	if err = dfaq.Select(drivefileancestor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) IDsX(ctx context.Context) []string {
	ids, err := dfaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dfaq *DriveFileAncestorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dfaq.ctx, ent.OpQueryCount)
	if err := dfaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dfaq, querierCount[*DriveFileAncestorQuery](), dfaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) CountX(ctx context.Context) int {
	count, err := dfaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dfaq *DriveFileAncestorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dfaq.ctx, ent.OpQueryExist)
	switch _, err := dfaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dfaq *DriveFileAncestorQuery) ExistX(ctx context.Context) bool {
	exist, err := dfaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DriveFileAncestorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dfaq *DriveFileAncestorQuery) Clone() *DriveFileAncestorQuery {
	if dfaq == nil {
		return nil
	}
	return &DriveFileAncestorQuery{
		config:        dfaq.config,
		ctx:           dfaq.ctx.Clone(),
		order:         append([]drivefileancestor.OrderOption{}, dfaq.order...),
		inters:        append([]Interceptor{}, dfaq.inters...),
		predicates:    append([]predicate.DriveFileAncestor{}, dfaq.predicates...),
		withDriveFile: dfaq.withDriveFile.Clone(),
		// clone intermediate query.
		sql:  dfaq.sql.Clone(),
		path: dfaq.path,
	}
}

// WithDriveFile tells the query-builder to eager-load the nodes that are connected to
// the "drive_file" edge. The optional arguments are used to configure the query builder of the edge.
func (dfaq *DriveFileAncestorQuery) WithDriveFile(opts ...func(*DriveFileQuery)) *DriveFileAncestorQuery {
	query := (&DriveFileClient{config: dfaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dfaq.withDriveFile = query
	return dfaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FileRef string `json:"file_ref,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DriveFileAncestor.Query().
//		GroupBy(drivefileancestor.FieldFileRef).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dfaq *DriveFileAncestorQuery) GroupBy(field string, fields ...string) *DriveFileAncestorGroupBy {
	dfaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DriveFileAncestorGroupBy{build: dfaq}
	grbuild.flds = &dfaq.ctx.Fields
	grbuild.label = drivefileancestor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FileRef string `json:"file_ref,omitempty"`
//	}
//
//	client.DriveFileAncestor.Query().
//		Select(drivefileancestor.FieldFileRef).
//		Scan(ctx, &v)
func (dfaq *DriveFileAncestorQuery) Select(fields ...string) *DriveFileAncestorSelect {
	dfaq.ctx.Fields = append(dfaq.ctx.Fields, fields...)
	sbuild := &DriveFileAncestorSelect{DriveFileAncestorQuery: dfaq}
	sbuild.label = drivefileancestor.Label
	sbuild.flds, sbuild.scan = &dfaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DriveFileAncestorSelect configured with the given aggregations.
func (dfaq *DriveFileAncestorQuery) Aggregate(fns ...AggregateFunc) *DriveFileAncestorSelect {
	return dfaq.Select().Aggregate(fns...)
}

func (dfaq *DriveFileAncestorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dfaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dfaq); err != nil {
				return err
			}
		}
	}
	for _, f := range dfaq.ctx.Fields {
		if !drivefileancestor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dfaq.path != nil {
		prev, err := dfaq.path(ctx)
		if err != nil {
			return err
		}
		dfaq.sql = prev
	}
	return nil
}

func (dfaq *DriveFileAncestorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DriveFileAncestor, error) {
	var (
		nodes       = []*DriveFileAncestor{}
		_spec       = dfaq.querySpec()
		loadedTypes = [1]bool{
			dfaq.withDriveFile != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DriveFileAncestor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DriveFileAncestor{config: dfaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dfaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dfaq.withDriveFile; query != nil {
		if err := dfaq.loadDriveFile(ctx, query, nodes, nil,
			func(n *DriveFileAncestor, e *DriveFile) { n.Edges.DriveFile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dfaq *DriveFileAncestorQuery) loadDriveFile(ctx context.Context, query *DriveFileQuery, nodes []*DriveFileAncestor, init func(*DriveFileAncestor), assign func(*DriveFileAncestor, *DriveFile)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*DriveFileAncestor)
	for i := range nodes {
		fk := nodes[i].FileRef
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(drivefile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "file_ref" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dfaq *DriveFileAncestorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dfaq.querySpec()
	_spec.Node.Columns = dfaq.ctx.Fields
	if len(dfaq.ctx.Fields) > 0 {
		_spec.Unique = dfaq.ctx.Unique != nil && *dfaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dfaq.driver, _spec)
}

func (dfaq *DriveFileAncestorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(drivefileancestor.Table, drivefileancestor.Columns, sqlgraph.NewFieldSpec(drivefileancestor.FieldID, field.TypeString))
	_spec.From = dfaq.sql
	if unique := dfaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dfaq.path != nil {
		_spec.Unique = true
	}
	if fields := dfaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, drivefileancestor.FieldID)
		for i := range fields {
			if fields[i] != drivefileancestor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dfaq.withDriveFile != nil {
			_spec.Node.AddColumnOnce(drivefileancestor.FieldFileRef)
		}
	}
	if ps := dfaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dfaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dfaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dfaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dfaq *DriveFileAncestorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dfaq.driver.Dialect())
	t1 := builder.Table(drivefileancestor.Table)
	columns := dfaq.ctx.Fields
	if len(columns) == 0 {
		columns = drivefileancestor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dfaq.sql != nil {
		selector = dfaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dfaq.ctx.Unique != nil && *dfaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dfaq.predicates {
		p(selector)
	}
	for _, p := range dfaq.order {
		p(selector)
	}
	if offset := dfaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dfaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DriveFileAncestorGroupBy is the group-by builder for DriveFileAncestor entities.
type DriveFileAncestorGroupBy struct {
	selector
	build *DriveFileAncestorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dfagb *DriveFileAncestorGroupBy) Aggregate(fns ...AggregateFunc) *DriveFileAncestorGroupBy {
	dfagb.fns = append(dfagb.fns, fns...)
	return dfagb
}

// Scan applies the selector query and scans the result into the given value.
func (dfagb *DriveFileAncestorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dfagb.build.ctx, ent.OpQueryGroupBy)
	if err := dfagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DriveFileAncestorQuery, *DriveFileAncestorGroupBy](ctx, dfagb.build, dfagb, dfagb.build.inters, v)
}

func (dfagb *DriveFileAncestorGroupBy) sqlScan(ctx context.Context, root *DriveFileAncestorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dfagb.fns))
	for _, fn := range dfagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dfagb.flds)+len(dfagb.fns))
		for _, f := range *dfagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dfagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dfagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DriveFileAncestorSelect is the builder for selecting fields of DriveFileAncestor entities.
type DriveFileAncestorSelect struct {
	*DriveFileAncestorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dfas *DriveFileAncestorSelect) Aggregate(fns ...AggregateFunc) *DriveFileAncestorSelect {
	dfas.fns = append(dfas.fns, fns...)
	return dfas
}

// Scan applies the selector query and scans the result into the given value.
func (dfas *DriveFileAncestorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dfas.ctx, ent.OpQuerySelect)
	if err := dfas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DriveFileAncestorQuery, *DriveFileAncestorSelect](ctx, dfas.DriveFileAncestorQuery, dfas, dfas.inters, v)
}

func (dfas *DriveFileAncestorSelect) sqlScan(ctx context.Context, root *DriveFileAncestorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dfas.fns))
	for _, fn := range dfas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dfas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dfas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
