// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefile"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefilestar"
)

// DriveFileStar is the model entity for the DriveFileStar schema.
type DriveFileStar struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Etag holds the value of the "etag" field.
	Etag string `json:"etag,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// FileRef holds the value of the "file_ref" field.
	FileRef string `json:"file_ref,omitempty"`
	// UserRef holds the value of the "user_ref" field.
	UserRef string `json:"user_ref,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DriveFileStarQuery when eager-loading is set.
	Edges        DriveFileStarEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DriveFileStarEdges holds the relations/edges for other nodes in the graph.
type DriveFileStarEdges struct {
	// DriveFiles holds the value of the drive_files edge.
	DriveFiles *DriveFile `json:"drive_files,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DriveFilesOrErr returns the DriveFiles value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DriveFileStarEdges) DriveFilesOrErr() (*DriveFile, error) {
	if e.DriveFiles != nil {
		return e.DriveFiles, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: drivefile.Label}
	}
	return nil, &NotLoadedError{edge: "drive_files"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DriveFileStar) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case drivefilestar.FieldID, drivefilestar.FieldEtag, drivefilestar.FieldFileRef, drivefilestar.FieldUserRef:
			values[i] = new(sql.NullString)
		case drivefilestar.FieldCreatedAt, drivefilestar.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DriveFileStar fields.
func (dfs *DriveFileStar) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case drivefilestar.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				dfs.ID = value.String
			}
		case drivefilestar.FieldEtag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field etag", values[i])
			} else if value.Valid {
				dfs.Etag = value.String
			}
		case drivefilestar.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dfs.CreatedAt = value.Time
			}
		case drivefilestar.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dfs.UpdatedAt = value.Time
			}
		case drivefilestar.FieldFileRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_ref", values[i])
			} else if value.Valid {
				dfs.FileRef = value.String
			}
		case drivefilestar.FieldUserRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_ref", values[i])
			} else if value.Valid {
				dfs.UserRef = value.String
			}
		default:
			dfs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DriveFileStar.
// This includes values selected through modifiers, order, etc.
func (dfs *DriveFileStar) Value(name string) (ent.Value, error) {
	return dfs.selectValues.Get(name)
}

// QueryDriveFiles queries the "drive_files" edge of the DriveFileStar entity.
func (dfs *DriveFileStar) QueryDriveFiles() *DriveFileQuery {
	return NewDriveFileStarClient(dfs.config).QueryDriveFiles(dfs)
}

// Update returns a builder for updating this DriveFileStar.
// Note that you need to call DriveFileStar.Unwrap() before calling this method if this DriveFileStar
// was returned from a transaction, and the transaction was committed or rolled back.
func (dfs *DriveFileStar) Update() *DriveFileStarUpdateOne {
	return NewDriveFileStarClient(dfs.config).UpdateOne(dfs)
}

// Unwrap unwraps the DriveFileStar entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dfs *DriveFileStar) Unwrap() *DriveFileStar {
	_tx, ok := dfs.config.driver.(*txDriver)
	if !ok {
		panic("ent: DriveFileStar is not a transactional entity")
	}
	dfs.config.driver = _tx.drv
	return dfs
}

// String implements the fmt.Stringer.
func (dfs *DriveFileStar) String() string {
	var builder strings.Builder
	builder.WriteString("DriveFileStar(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dfs.ID))
	builder.WriteString("etag=")
	builder.WriteString(dfs.Etag)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dfs.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dfs.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("file_ref=")
	builder.WriteString(dfs.FileRef)
	builder.WriteString(", ")
	builder.WriteString("user_ref=")
	builder.WriteString(dfs.UserRef)
	builder.WriteByte(')')
	return builder.String()
}

// DriveFileStars is a parsable slice of DriveFileStar.
type DriveFileStars []*DriveFileStar
