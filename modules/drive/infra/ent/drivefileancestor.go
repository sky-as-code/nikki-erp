// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefile"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefileancestor"
)

// DriveFileAncestor is the model entity for the DriveFileAncestor schema.
type DriveFileAncestor struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// FileRef holds the value of the "file_ref" field.
	FileRef string `json:"file_ref,omitempty"`
	// AncestorRef holds the value of the "ancestor_ref" field.
	AncestorRef string `json:"ancestor_ref,omitempty"`
	// Depth holds the value of the "depth" field.
	Depth int `json:"depth,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DriveFileAncestorQuery when eager-loading is set.
	Edges        DriveFileAncestorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DriveFileAncestorEdges holds the relations/edges for other nodes in the graph.
type DriveFileAncestorEdges struct {
	// DriveFile holds the value of the drive_file edge.
	DriveFile *DriveFile `json:"drive_file,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DriveFileOrErr returns the DriveFile value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DriveFileAncestorEdges) DriveFileOrErr() (*DriveFile, error) {
	if e.DriveFile != nil {
		return e.DriveFile, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: drivefile.Label}
	}
	return nil, &NotLoadedError{edge: "drive_file"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DriveFileAncestor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case drivefileancestor.FieldDepth:
			values[i] = new(sql.NullInt64)
		case drivefileancestor.FieldID, drivefileancestor.FieldFileRef, drivefileancestor.FieldAncestorRef:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DriveFileAncestor fields.
func (dfa *DriveFileAncestor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case drivefileancestor.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				dfa.ID = value.String
			}
		case drivefileancestor.FieldFileRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_ref", values[i])
			} else if value.Valid {
				dfa.FileRef = value.String
			}
		case drivefileancestor.FieldAncestorRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ancestor_ref", values[i])
			} else if value.Valid {
				dfa.AncestorRef = value.String
			}
		case drivefileancestor.FieldDepth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field depth", values[i])
			} else if value.Valid {
				dfa.Depth = int(value.Int64)
			}
		default:
			dfa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DriveFileAncestor.
// This includes values selected through modifiers, order, etc.
func (dfa *DriveFileAncestor) Value(name string) (ent.Value, error) {
	return dfa.selectValues.Get(name)
}

// QueryDriveFile queries the "drive_file" edge of the DriveFileAncestor entity.
func (dfa *DriveFileAncestor) QueryDriveFile() *DriveFileQuery {
	return NewDriveFileAncestorClient(dfa.config).QueryDriveFile(dfa)
}

// Update returns a builder for updating this DriveFileAncestor.
// Note that you need to call DriveFileAncestor.Unwrap() before calling this method if this DriveFileAncestor
// was returned from a transaction, and the transaction was committed or rolled back.
func (dfa *DriveFileAncestor) Update() *DriveFileAncestorUpdateOne {
	return NewDriveFileAncestorClient(dfa.config).UpdateOne(dfa)
}

// Unwrap unwraps the DriveFileAncestor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dfa *DriveFileAncestor) Unwrap() *DriveFileAncestor {
	_tx, ok := dfa.config.driver.(*txDriver)
	if !ok {
		panic("ent: DriveFileAncestor is not a transactional entity")
	}
	dfa.config.driver = _tx.drv
	return dfa
}

// String implements the fmt.Stringer.
func (dfa *DriveFileAncestor) String() string {
	var builder strings.Builder
	builder.WriteString("DriveFileAncestor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dfa.ID))
	builder.WriteString("file_ref=")
	builder.WriteString(dfa.FileRef)
	builder.WriteString(", ")
	builder.WriteString("ancestor_ref=")
	builder.WriteString(dfa.AncestorRef)
	builder.WriteString(", ")
	builder.WriteString("depth=")
	builder.WriteString(fmt.Sprintf("%v", dfa.Depth))
	builder.WriteByte(')')
	return builder.String()
}

// DriveFileAncestors is a parsable slice of DriveFileAncestor.
type DriveFileAncestors []*DriveFileAncestor
