// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefile"
	"github.com/sky-as-code/nikki-erp/modules/drive/infra/ent/drivefileshare"
)

// DriveFileShare is the model entity for the DriveFileShare schema.
type DriveFileShare struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Etag holds the value of the "etag" field.
	Etag string `json:"etag,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// FileRef holds the value of the "file_ref" field.
	FileRef string `json:"file_ref,omitempty"`
	// UserRef holds the value of the "user_ref" field.
	UserRef string `json:"user_ref,omitempty"`
	// Permission holds the value of the "permission" field.
	Permission string `json:"permission,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DriveFileShareQuery when eager-loading is set.
	Edges        DriveFileShareEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DriveFileShareEdges holds the relations/edges for other nodes in the graph.
type DriveFileShareEdges struct {
	// DriveFiles holds the value of the drive_files edge.
	DriveFiles *DriveFile `json:"drive_files,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DriveFilesOrErr returns the DriveFiles value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DriveFileShareEdges) DriveFilesOrErr() (*DriveFile, error) {
	if e.DriveFiles != nil {
		return e.DriveFiles, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: drivefile.Label}
	}
	return nil, &NotLoadedError{edge: "drive_files"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DriveFileShare) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case drivefileshare.FieldID, drivefileshare.FieldEtag, drivefileshare.FieldFileRef, drivefileshare.FieldUserRef, drivefileshare.FieldPermission:
			values[i] = new(sql.NullString)
		case drivefileshare.FieldCreatedAt, drivefileshare.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DriveFileShare fields.
func (dfs *DriveFileShare) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case drivefileshare.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				dfs.ID = value.String
			}
		case drivefileshare.FieldEtag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field etag", values[i])
			} else if value.Valid {
				dfs.Etag = value.String
			}
		case drivefileshare.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dfs.CreatedAt = value.Time
			}
		case drivefileshare.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dfs.UpdatedAt = value.Time
			}
		case drivefileshare.FieldFileRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_ref", values[i])
			} else if value.Valid {
				dfs.FileRef = value.String
			}
		case drivefileshare.FieldUserRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_ref", values[i])
			} else if value.Valid {
				dfs.UserRef = value.String
			}
		case drivefileshare.FieldPermission:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission", values[i])
			} else if value.Valid {
				dfs.Permission = value.String
			}
		default:
			dfs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DriveFileShare.
// This includes values selected through modifiers, order, etc.
func (dfs *DriveFileShare) Value(name string) (ent.Value, error) {
	return dfs.selectValues.Get(name)
}

// QueryDriveFiles queries the "drive_files" edge of the DriveFileShare entity.
func (dfs *DriveFileShare) QueryDriveFiles() *DriveFileQuery {
	return NewDriveFileShareClient(dfs.config).QueryDriveFiles(dfs)
}

// Update returns a builder for updating this DriveFileShare.
// Note that you need to call DriveFileShare.Unwrap() before calling this method if this DriveFileShare
// was returned from a transaction, and the transaction was committed or rolled back.
func (dfs *DriveFileShare) Update() *DriveFileShareUpdateOne {
	return NewDriveFileShareClient(dfs.config).UpdateOne(dfs)
}

// Unwrap unwraps the DriveFileShare entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dfs *DriveFileShare) Unwrap() *DriveFileShare {
	_tx, ok := dfs.config.driver.(*txDriver)
	if !ok {
		panic("ent: DriveFileShare is not a transactional entity")
	}
	dfs.config.driver = _tx.drv
	return dfs
}

// String implements the fmt.Stringer.
func (dfs *DriveFileShare) String() string {
	var builder strings.Builder
	builder.WriteString("DriveFileShare(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dfs.ID))
	builder.WriteString("etag=")
	builder.WriteString(dfs.Etag)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dfs.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dfs.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("file_ref=")
	builder.WriteString(dfs.FileRef)
	builder.WriteString(", ")
	builder.WriteString("user_ref=")
	builder.WriteString(dfs.UserRef)
	builder.WriteString(", ")
	builder.WriteString("permission=")
	builder.WriteString(dfs.Permission)
	builder.WriteByte(')')
	return builder.String()
}

// DriveFileShares is a parsable slice of DriveFileShare.
type DriveFileShares []*DriveFileShare
