// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/essential/infra/ent/module"
	"github.com/sky-as-code/nikki-erp/modules/essential/infra/ent/moduleorgrel"
)

// ModuleOrgRel is the model entity for the ModuleOrgRel schema.
type ModuleOrgRel struct {
	config `json:"-"`
	// ID of the ent.
	// Not used. Just because Ent requires an ID field.
	ID string `json:"id,omitempty"`
	// ModuleID holds the value of the "module_id" field.
	ModuleID string `json:"module_id,omitempty"`
	// OrgID holds the value of the "org_id" field.
	OrgID string `json:"org_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModuleOrgRelQuery when eager-loading is set.
	Edges        ModuleOrgRelEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ModuleOrgRelEdges holds the relations/edges for other nodes in the graph.
type ModuleOrgRelEdges struct {
	// Module holds the value of the module edge.
	Module *Module `json:"module,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ModuleOrErr returns the Module value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ModuleOrgRelEdges) ModuleOrErr() (*Module, error) {
	if e.Module != nil {
		return e.Module, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: module.Label}
	}
	return nil, &NotLoadedError{edge: "module"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ModuleOrgRel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case moduleorgrel.FieldID, moduleorgrel.FieldModuleID, moduleorgrel.FieldOrgID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ModuleOrgRel fields.
func (mor *ModuleOrgRel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case moduleorgrel.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				mor.ID = value.String
			}
		case moduleorgrel.FieldModuleID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field module_id", values[i])
			} else if value.Valid {
				mor.ModuleID = value.String
			}
		case moduleorgrel.FieldOrgID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field org_id", values[i])
			} else if value.Valid {
				mor.OrgID = value.String
			}
		default:
			mor.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ModuleOrgRel.
// This includes values selected through modifiers, order, etc.
func (mor *ModuleOrgRel) Value(name string) (ent.Value, error) {
	return mor.selectValues.Get(name)
}

// QueryModule queries the "module" edge of the ModuleOrgRel entity.
func (mor *ModuleOrgRel) QueryModule() *ModuleQuery {
	return NewModuleOrgRelClient(mor.config).QueryModule(mor)
}

// Update returns a builder for updating this ModuleOrgRel.
// Note that you need to call ModuleOrgRel.Unwrap() before calling this method if this ModuleOrgRel
// was returned from a transaction, and the transaction was committed or rolled back.
func (mor *ModuleOrgRel) Update() *ModuleOrgRelUpdateOne {
	return NewModuleOrgRelClient(mor.config).UpdateOne(mor)
}

// Unwrap unwraps the ModuleOrgRel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mor *ModuleOrgRel) Unwrap() *ModuleOrgRel {
	_tx, ok := mor.config.driver.(*txDriver)
	if !ok {
		panic("ent: ModuleOrgRel is not a transactional entity")
	}
	mor.config.driver = _tx.drv
	return mor
}

// String implements the fmt.Stringer.
func (mor *ModuleOrgRel) String() string {
	var builder strings.Builder
	builder.WriteString("ModuleOrgRel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mor.ID))
	builder.WriteString("module_id=")
	builder.WriteString(mor.ModuleID)
	builder.WriteString(", ")
	builder.WriteString("org_id=")
	builder.WriteString(mor.OrgID)
	builder.WriteByte(')')
	return builder.String()
}

// ModuleOrgRels is a parsable slice of ModuleOrgRel.
type ModuleOrgRels []*ModuleOrgRel
