// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sky-as-code/nikki-erp/modules/contacts/infra/ent/commchannel"
	"github.com/sky-as-code/nikki-erp/modules/contacts/infra/ent/party"
	"github.com/sky-as-code/nikki-erp/modules/contacts/infra/ent/predicate"
	"github.com/sky-as-code/nikki-erp/modules/contacts/infra/ent/relationship"
)

// PartyQuery is the builder for querying Party entities.
type PartyQuery struct {
	config
	ctx               *QueryContext
	order             []party.OrderOption
	inters            []Interceptor
	predicates        []predicate.Party
	withCommChannels  *CommChannelQuery
	withRelationships *RelationshipQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartyQuery builder.
func (pq *PartyQuery) Where(ps ...predicate.Party) *PartyQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *PartyQuery) Limit(limit int) *PartyQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *PartyQuery) Offset(offset int) *PartyQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *PartyQuery) Unique(unique bool) *PartyQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *PartyQuery) Order(o ...party.OrderOption) *PartyQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryCommChannels chains the current query on the "comm_channels" edge.
func (pq *PartyQuery) QueryCommChannels() *CommChannelQuery {
	query := (&CommChannelClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(party.Table, party.FieldID, selector),
			sqlgraph.To(commchannel.Table, commchannel.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, party.CommChannelsTable, party.CommChannelsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRelationships chains the current query on the "relationships" edge.
func (pq *PartyQuery) QueryRelationships() *RelationshipQuery {
	query := (&RelationshipClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(party.Table, party.FieldID, selector),
			sqlgraph.To(relationship.Table, relationship.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, party.RelationshipsTable, party.RelationshipsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Party entity from the query.
// Returns a *NotFoundError when no Party was found.
func (pq *PartyQuery) First(ctx context.Context) (*Party, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{party.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PartyQuery) FirstX(ctx context.Context) *Party {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Party ID from the query.
// Returns a *NotFoundError when no Party ID was found.
func (pq *PartyQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{party.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PartyQuery) FirstIDX(ctx context.Context) string {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Party entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Party entity is found.
// Returns a *NotFoundError when no Party entities are found.
func (pq *PartyQuery) Only(ctx context.Context) (*Party, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{party.Label}
	default:
		return nil, &NotSingularError{party.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PartyQuery) OnlyX(ctx context.Context) *Party {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Party ID in the query.
// Returns a *NotSingularError when more than one Party ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *PartyQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{party.Label}
	default:
		err = &NotSingularError{party.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PartyQuery) OnlyIDX(ctx context.Context) string {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Parties.
func (pq *PartyQuery) All(ctx context.Context) ([]*Party, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryAll)
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Party, *PartyQuery]()
	return withInterceptors[[]*Party](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *PartyQuery) AllX(ctx context.Context) []*Party {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Party IDs.
func (pq *PartyQuery) IDs(ctx context.Context) (ids []string, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryIDs)
	if err = pq.Select(party.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PartyQuery) IDsX(ctx context.Context) []string {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PartyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryCount)
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*PartyQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PartyQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PartyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, ent.OpQueryExist)
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PartyQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PartyQuery) Clone() *PartyQuery {
	if pq == nil {
		return nil
	}
	return &PartyQuery{
		config:            pq.config,
		ctx:               pq.ctx.Clone(),
		order:             append([]party.OrderOption{}, pq.order...),
		inters:            append([]Interceptor{}, pq.inters...),
		predicates:        append([]predicate.Party{}, pq.predicates...),
		withCommChannels:  pq.withCommChannels.Clone(),
		withRelationships: pq.withRelationships.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithCommChannels tells the query-builder to eager-load the nodes that are connected to
// the "comm_channels" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PartyQuery) WithCommChannels(opts ...func(*CommChannelQuery)) *PartyQuery {
	query := (&CommChannelClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withCommChannels = query
	return pq
}

// WithRelationships tells the query-builder to eager-load the nodes that are connected to
// the "relationships" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PartyQuery) WithRelationships(opts ...func(*RelationshipQuery)) *PartyQuery {
	query := (&RelationshipClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withRelationships = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AvatarURL string `json:"avatar_url,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Party.Query().
//		GroupBy(party.FieldAvatarURL).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *PartyQuery) GroupBy(field string, fields ...string) *PartyGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PartyGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = party.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AvatarURL string `json:"avatar_url,omitempty"`
//	}
//
//	client.Party.Query().
//		Select(party.FieldAvatarURL).
//		Scan(ctx, &v)
func (pq *PartyQuery) Select(fields ...string) *PartySelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &PartySelect{PartyQuery: pq}
	sbuild.label = party.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PartySelect configured with the given aggregations.
func (pq *PartyQuery) Aggregate(fns ...AggregateFunc) *PartySelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *PartyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !party.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PartyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Party, error) {
	var (
		nodes       = []*Party{}
		_spec       = pq.querySpec()
		loadedTypes = [2]bool{
			pq.withCommChannels != nil,
			pq.withRelationships != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Party).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Party{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withCommChannels; query != nil {
		if err := pq.loadCommChannels(ctx, query, nodes,
			func(n *Party) { n.Edges.CommChannels = []*CommChannel{} },
			func(n *Party, e *CommChannel) { n.Edges.CommChannels = append(n.Edges.CommChannels, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withRelationships; query != nil {
		if err := pq.loadRelationships(ctx, query, nodes,
			func(n *Party) { n.Edges.Relationships = []*Relationship{} },
			func(n *Party, e *Relationship) { n.Edges.Relationships = append(n.Edges.Relationships, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *PartyQuery) loadCommChannels(ctx context.Context, query *CommChannelQuery, nodes []*Party, init func(*Party), assign func(*Party, *CommChannel)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Party)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(commchannel.FieldPartyID)
	}
	query.Where(predicate.CommChannel(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(party.CommChannelsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PartyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "party_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *PartyQuery) loadRelationships(ctx context.Context, query *RelationshipQuery, nodes []*Party, init func(*Party), assign func(*Party, *Relationship)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Party)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(relationship.FieldTargetPartyID)
	}
	query.Where(predicate.Relationship(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(party.RelationshipsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TargetPartyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "target_party_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (pq *PartyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PartyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(party.Table, party.Columns, sqlgraph.NewFieldSpec(party.FieldID, field.TypeString))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, party.FieldID)
		for i := range fields {
			if fields[i] != party.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *PartyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(party.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = party.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PartyGroupBy is the group-by builder for Party entities.
type PartyGroupBy struct {
	selector
	build *PartyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PartyGroupBy) Aggregate(fns ...AggregateFunc) *PartyGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *PartyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, ent.OpQueryGroupBy)
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartyQuery, *PartyGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *PartyGroupBy) sqlScan(ctx context.Context, root *PartyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PartySelect is the builder for selecting fields of Party entities.
type PartySelect struct {
	*PartyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *PartySelect) Aggregate(fns ...AggregateFunc) *PartySelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PartySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, ent.OpQuerySelect)
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartyQuery, *PartySelect](ctx, ps.PartyQuery, ps, ps.inters, v)
}

func (ps *PartySelect) sqlScan(ctx context.Context, root *PartyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
