// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sky-as-code/nikki-erp/modules/authenticate/infra/ent/authnenum"
	"github.com/sky-as-code/nikki-erp/modules/authenticate/infra/ent/predicate"
)

// AuthNEnumQuery is the builder for querying AuthNEnum entities.
type AuthNEnumQuery struct {
	config
	ctx        *QueryContext
	order      []authnenum.OrderOption
	inters     []Interceptor
	predicates []predicate.AuthNEnum
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AuthNEnumQuery builder.
func (anq *AuthNEnumQuery) Where(ps ...predicate.AuthNEnum) *AuthNEnumQuery {
	anq.predicates = append(anq.predicates, ps...)
	return anq
}

// Limit the number of records to be returned by this query.
func (anq *AuthNEnumQuery) Limit(limit int) *AuthNEnumQuery {
	anq.ctx.Limit = &limit
	return anq
}

// Offset to start from.
func (anq *AuthNEnumQuery) Offset(offset int) *AuthNEnumQuery {
	anq.ctx.Offset = &offset
	return anq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (anq *AuthNEnumQuery) Unique(unique bool) *AuthNEnumQuery {
	anq.ctx.Unique = &unique
	return anq
}

// Order specifies how the records should be ordered.
func (anq *AuthNEnumQuery) Order(o ...authnenum.OrderOption) *AuthNEnumQuery {
	anq.order = append(anq.order, o...)
	return anq
}

// First returns the first AuthNEnum entity from the query.
// Returns a *NotFoundError when no AuthNEnum was found.
func (anq *AuthNEnumQuery) First(ctx context.Context) (*AuthNEnum, error) {
	nodes, err := anq.Limit(1).All(setContextOp(ctx, anq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{authnenum.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (anq *AuthNEnumQuery) FirstX(ctx context.Context) *AuthNEnum {
	node, err := anq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AuthNEnum ID from the query.
// Returns a *NotFoundError when no AuthNEnum ID was found.
func (anq *AuthNEnumQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = anq.Limit(1).IDs(setContextOp(ctx, anq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{authnenum.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (anq *AuthNEnumQuery) FirstIDX(ctx context.Context) string {
	id, err := anq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AuthNEnum entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AuthNEnum entity is found.
// Returns a *NotFoundError when no AuthNEnum entities are found.
func (anq *AuthNEnumQuery) Only(ctx context.Context) (*AuthNEnum, error) {
	nodes, err := anq.Limit(2).All(setContextOp(ctx, anq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{authnenum.Label}
	default:
		return nil, &NotSingularError{authnenum.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (anq *AuthNEnumQuery) OnlyX(ctx context.Context) *AuthNEnum {
	node, err := anq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AuthNEnum ID in the query.
// Returns a *NotSingularError when more than one AuthNEnum ID is found.
// Returns a *NotFoundError when no entities are found.
func (anq *AuthNEnumQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = anq.Limit(2).IDs(setContextOp(ctx, anq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{authnenum.Label}
	default:
		err = &NotSingularError{authnenum.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (anq *AuthNEnumQuery) OnlyIDX(ctx context.Context) string {
	id, err := anq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AuthNEnums.
func (anq *AuthNEnumQuery) All(ctx context.Context) ([]*AuthNEnum, error) {
	ctx = setContextOp(ctx, anq.ctx, ent.OpQueryAll)
	if err := anq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AuthNEnum, *AuthNEnumQuery]()
	return withInterceptors[[]*AuthNEnum](ctx, anq, qr, anq.inters)
}

// AllX is like All, but panics if an error occurs.
func (anq *AuthNEnumQuery) AllX(ctx context.Context) []*AuthNEnum {
	nodes, err := anq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AuthNEnum IDs.
func (anq *AuthNEnumQuery) IDs(ctx context.Context) (ids []string, err error) {
	if anq.ctx.Unique == nil && anq.path != nil {
		anq.Unique(true)
	}
	ctx = setContextOp(ctx, anq.ctx, ent.OpQueryIDs)
	if err = anq.Select(authnenum.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (anq *AuthNEnumQuery) IDsX(ctx context.Context) []string {
	ids, err := anq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (anq *AuthNEnumQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, anq.ctx, ent.OpQueryCount)
	if err := anq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, anq, querierCount[*AuthNEnumQuery](), anq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (anq *AuthNEnumQuery) CountX(ctx context.Context) int {
	count, err := anq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (anq *AuthNEnumQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, anq.ctx, ent.OpQueryExist)
	switch _, err := anq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (anq *AuthNEnumQuery) ExistX(ctx context.Context) bool {
	exist, err := anq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AuthNEnumQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (anq *AuthNEnumQuery) Clone() *AuthNEnumQuery {
	if anq == nil {
		return nil
	}
	return &AuthNEnumQuery{
		config:     anq.config,
		ctx:        anq.ctx.Clone(),
		order:      append([]authnenum.OrderOption{}, anq.order...),
		inters:     append([]Interceptor{}, anq.inters...),
		predicates: append([]predicate.AuthNEnum{}, anq.predicates...),
		// clone intermediate query.
		sql:  anq.sql.Clone(),
		path: anq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Etag string `json:"etag,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AuthNEnum.Query().
//		GroupBy(authnenum.FieldEtag).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (anq *AuthNEnumQuery) GroupBy(field string, fields ...string) *AuthNEnumGroupBy {
	anq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AuthNEnumGroupBy{build: anq}
	grbuild.flds = &anq.ctx.Fields
	grbuild.label = authnenum.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Etag string `json:"etag,omitempty"`
//	}
//
//	client.AuthNEnum.Query().
//		Select(authnenum.FieldEtag).
//		Scan(ctx, &v)
func (anq *AuthNEnumQuery) Select(fields ...string) *AuthNEnumSelect {
	anq.ctx.Fields = append(anq.ctx.Fields, fields...)
	sbuild := &AuthNEnumSelect{AuthNEnumQuery: anq}
	sbuild.label = authnenum.Label
	sbuild.flds, sbuild.scan = &anq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AuthNEnumSelect configured with the given aggregations.
func (anq *AuthNEnumQuery) Aggregate(fns ...AggregateFunc) *AuthNEnumSelect {
	return anq.Select().Aggregate(fns...)
}

func (anq *AuthNEnumQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range anq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, anq); err != nil {
				return err
			}
		}
	}
	for _, f := range anq.ctx.Fields {
		if !authnenum.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if anq.path != nil {
		prev, err := anq.path(ctx)
		if err != nil {
			return err
		}
		anq.sql = prev
	}
	return nil
}

func (anq *AuthNEnumQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AuthNEnum, error) {
	var (
		nodes = []*AuthNEnum{}
		_spec = anq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AuthNEnum).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AuthNEnum{config: anq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, anq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (anq *AuthNEnumQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := anq.querySpec()
	_spec.Node.Columns = anq.ctx.Fields
	if len(anq.ctx.Fields) > 0 {
		_spec.Unique = anq.ctx.Unique != nil && *anq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, anq.driver, _spec)
}

func (anq *AuthNEnumQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(authnenum.Table, authnenum.Columns, sqlgraph.NewFieldSpec(authnenum.FieldID, field.TypeString))
	_spec.From = anq.sql
	if unique := anq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if anq.path != nil {
		_spec.Unique = true
	}
	if fields := anq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, authnenum.FieldID)
		for i := range fields {
			if fields[i] != authnenum.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := anq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := anq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := anq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := anq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (anq *AuthNEnumQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(anq.driver.Dialect())
	t1 := builder.Table(authnenum.Table)
	columns := anq.ctx.Fields
	if len(columns) == 0 {
		columns = authnenum.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if anq.sql != nil {
		selector = anq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if anq.ctx.Unique != nil && *anq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range anq.predicates {
		p(selector)
	}
	for _, p := range anq.order {
		p(selector)
	}
	if offset := anq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := anq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AuthNEnumGroupBy is the group-by builder for AuthNEnum entities.
type AuthNEnumGroupBy struct {
	selector
	build *AuthNEnumQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (angb *AuthNEnumGroupBy) Aggregate(fns ...AggregateFunc) *AuthNEnumGroupBy {
	angb.fns = append(angb.fns, fns...)
	return angb
}

// Scan applies the selector query and scans the result into the given value.
func (angb *AuthNEnumGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, angb.build.ctx, ent.OpQueryGroupBy)
	if err := angb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AuthNEnumQuery, *AuthNEnumGroupBy](ctx, angb.build, angb, angb.build.inters, v)
}

func (angb *AuthNEnumGroupBy) sqlScan(ctx context.Context, root *AuthNEnumQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(angb.fns))
	for _, fn := range angb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*angb.flds)+len(angb.fns))
		for _, f := range *angb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*angb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := angb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AuthNEnumSelect is the builder for selecting fields of AuthNEnum entities.
type AuthNEnumSelect struct {
	*AuthNEnumQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ans *AuthNEnumSelect) Aggregate(fns ...AggregateFunc) *AuthNEnumSelect {
	ans.fns = append(ans.fns, fns...)
	return ans
}

// Scan applies the selector query and scans the result into the given value.
func (ans *AuthNEnumSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ans.ctx, ent.OpQuerySelect)
	if err := ans.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AuthNEnumQuery, *AuthNEnumSelect](ctx, ans.AuthNEnumQuery, ans, ans.inters, v)
}

func (ans *AuthNEnumSelect) sqlScan(ctx context.Context, root *AuthNEnumQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ans.fns))
	for _, fn := range ans.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ans.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ans.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
