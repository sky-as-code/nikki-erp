// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/common/model"
	"github.com/sky-as-code/nikki-erp/modules/authenticate/infra/ent/authnenum"
)

// AuthNEnum is the model entity for the AuthNEnum schema.
type AuthNEnum struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Etag holds the value of the "etag" field.
	Etag string `json:"etag,omitempty"`
	// Label holds the value of the "label" field.
	Label model.LangJson `json:"label,omitempty"`
	// Value holds the value of the "value" field.
	Value *string `json:"value,omitempty"`
	// Type holds the value of the "type" field.
	Type         string `json:"type,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthNEnum) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authnenum.FieldLabel:
			values[i] = new([]byte)
		case authnenum.FieldID, authnenum.FieldEtag, authnenum.FieldValue, authnenum.FieldType:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthNEnum fields.
func (an *AuthNEnum) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authnenum.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				an.ID = value.String
			}
		case authnenum.FieldEtag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field etag", values[i])
			} else if value.Valid {
				an.Etag = value.String
			}
		case authnenum.FieldLabel:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &an.Label); err != nil {
					return fmt.Errorf("unmarshal field label: %w", err)
				}
			}
		case authnenum.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				an.Value = new(string)
				*an.Value = value.String
			}
		case authnenum.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				an.Type = value.String
			}
		default:
			an.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the AuthNEnum.
// This includes values selected through modifiers, order, etc.
func (an *AuthNEnum) GetValue(name string) (ent.Value, error) {
	return an.selectValues.Get(name)
}

// Update returns a builder for updating this AuthNEnum.
// Note that you need to call AuthNEnum.Unwrap() before calling this method if this AuthNEnum
// was returned from a transaction, and the transaction was committed or rolled back.
func (an *AuthNEnum) Update() *AuthNEnumUpdateOne {
	return NewAuthNEnumClient(an.config).UpdateOne(an)
}

// Unwrap unwraps the AuthNEnum entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (an *AuthNEnum) Unwrap() *AuthNEnum {
	_tx, ok := an.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthNEnum is not a transactional entity")
	}
	an.config.driver = _tx.drv
	return an
}

// String implements the fmt.Stringer.
func (an *AuthNEnum) String() string {
	var builder strings.Builder
	builder.WriteString("AuthNEnum(")
	builder.WriteString(fmt.Sprintf("id=%v, ", an.ID))
	builder.WriteString("etag=")
	builder.WriteString(an.Etag)
	builder.WriteString(", ")
	builder.WriteString("label=")
	builder.WriteString(fmt.Sprintf("%v", an.Label))
	builder.WriteString(", ")
	if v := an.Value; v != nil {
		builder.WriteString("value=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(an.Type)
	builder.WriteByte(')')
	return builder.String()
}

// AuthNEnums is a parsable slice of AuthNEnum.
type AuthNEnums []*AuthNEnum
