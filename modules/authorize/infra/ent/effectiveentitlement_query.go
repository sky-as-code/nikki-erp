// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/sky-as-code/nikki-erp/modules/authorize/infra/ent/effectiveentitlement"
	"github.com/sky-as-code/nikki-erp/modules/authorize/infra/ent/predicate"
)

// EffectiveEntitlementQuery is the builder for querying EffectiveEntitlement entities.
type EffectiveEntitlementQuery struct {
	config
	ctx        *QueryContext
	order      []effectiveentitlement.OrderOption
	inters     []Interceptor
	predicates []predicate.EffectiveEntitlement
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EffectiveEntitlementQuery builder.
func (eeq *EffectiveEntitlementQuery) Where(ps ...predicate.EffectiveEntitlement) *EffectiveEntitlementQuery {
	eeq.predicates = append(eeq.predicates, ps...)
	return eeq
}

// Limit the number of records to be returned by this query.
func (eeq *EffectiveEntitlementQuery) Limit(limit int) *EffectiveEntitlementQuery {
	eeq.ctx.Limit = &limit
	return eeq
}

// Offset to start from.
func (eeq *EffectiveEntitlementQuery) Offset(offset int) *EffectiveEntitlementQuery {
	eeq.ctx.Offset = &offset
	return eeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eeq *EffectiveEntitlementQuery) Unique(unique bool) *EffectiveEntitlementQuery {
	eeq.ctx.Unique = &unique
	return eeq
}

// Order specifies how the records should be ordered.
func (eeq *EffectiveEntitlementQuery) Order(o ...effectiveentitlement.OrderOption) *EffectiveEntitlementQuery {
	eeq.order = append(eeq.order, o...)
	return eeq
}

// First returns the first EffectiveEntitlement entity from the query.
// Returns a *NotFoundError when no EffectiveEntitlement was found.
func (eeq *EffectiveEntitlementQuery) First(ctx context.Context) (*EffectiveEntitlement, error) {
	nodes, err := eeq.Limit(1).All(setContextOp(ctx, eeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{effectiveentitlement.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eeq *EffectiveEntitlementQuery) FirstX(ctx context.Context) *EffectiveEntitlement {
	node, err := eeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single EffectiveEntitlement entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EffectiveEntitlement entity is found.
// Returns a *NotFoundError when no EffectiveEntitlement entities are found.
func (eeq *EffectiveEntitlementQuery) Only(ctx context.Context) (*EffectiveEntitlement, error) {
	nodes, err := eeq.Limit(2).All(setContextOp(ctx, eeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{effectiveentitlement.Label}
	default:
		return nil, &NotSingularError{effectiveentitlement.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eeq *EffectiveEntitlementQuery) OnlyX(ctx context.Context) *EffectiveEntitlement {
	node, err := eeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of EffectiveEntitlements.
func (eeq *EffectiveEntitlementQuery) All(ctx context.Context) ([]*EffectiveEntitlement, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryAll)
	if err := eeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EffectiveEntitlement, *EffectiveEntitlementQuery]()
	return withInterceptors[[]*EffectiveEntitlement](ctx, eeq, qr, eeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eeq *EffectiveEntitlementQuery) AllX(ctx context.Context) []*EffectiveEntitlement {
	nodes, err := eeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (eeq *EffectiveEntitlementQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryCount)
	if err := eeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eeq, querierCount[*EffectiveEntitlementQuery](), eeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eeq *EffectiveEntitlementQuery) CountX(ctx context.Context) int {
	count, err := eeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eeq *EffectiveEntitlementQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eeq.ctx, ent.OpQueryExist)
	switch _, err := eeq.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eeq *EffectiveEntitlementQuery) ExistX(ctx context.Context) bool {
	exist, err := eeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EffectiveEntitlementQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eeq *EffectiveEntitlementQuery) Clone() *EffectiveEntitlementQuery {
	if eeq == nil {
		return nil
	}
	return &EffectiveEntitlementQuery{
		config:     eeq.config,
		ctx:        eeq.ctx.Clone(),
		order:      append([]effectiveentitlement.OrderOption{}, eeq.order...),
		inters:     append([]Interceptor{}, eeq.inters...),
		predicates: append([]predicate.EffectiveEntitlement{}, eeq.predicates...),
		// clone intermediate query.
		sql:  eeq.sql.Clone(),
		path: eeq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EffectiveEntitlement.Query().
//		GroupBy(effectiveentitlement.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eeq *EffectiveEntitlementQuery) GroupBy(field string, fields ...string) *EffectiveEntitlementGroupBy {
	eeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EffectiveEntitlementGroupBy{build: eeq}
	grbuild.flds = &eeq.ctx.Fields
	grbuild.label = effectiveentitlement.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//	}
//
//	client.EffectiveEntitlement.Query().
//		Select(effectiveentitlement.FieldUserID).
//		Scan(ctx, &v)
func (eeq *EffectiveEntitlementQuery) Select(fields ...string) *EffectiveEntitlementSelect {
	eeq.ctx.Fields = append(eeq.ctx.Fields, fields...)
	sbuild := &EffectiveEntitlementSelect{EffectiveEntitlementQuery: eeq}
	sbuild.label = effectiveentitlement.Label
	sbuild.flds, sbuild.scan = &eeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EffectiveEntitlementSelect configured with the given aggregations.
func (eeq *EffectiveEntitlementQuery) Aggregate(fns ...AggregateFunc) *EffectiveEntitlementSelect {
	return eeq.Select().Aggregate(fns...)
}

func (eeq *EffectiveEntitlementQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eeq); err != nil {
				return err
			}
		}
	}
	for _, f := range eeq.ctx.Fields {
		if !effectiveentitlement.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eeq.path != nil {
		prev, err := eeq.path(ctx)
		if err != nil {
			return err
		}
		eeq.sql = prev
	}
	return nil
}

func (eeq *EffectiveEntitlementQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EffectiveEntitlement, error) {
	var (
		nodes = []*EffectiveEntitlement{}
		_spec = eeq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EffectiveEntitlement).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EffectiveEntitlement{config: eeq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eeq *EffectiveEntitlementQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eeq.querySpec()
	_spec.Node.Columns = eeq.ctx.Fields
	if len(eeq.ctx.Fields) > 0 {
		_spec.Unique = eeq.ctx.Unique != nil && *eeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eeq.driver, _spec)
}

func (eeq *EffectiveEntitlementQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(effectiveentitlement.Table, effectiveentitlement.Columns, nil)
	_spec.From = eeq.sql
	if unique := eeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eeq.path != nil {
		_spec.Unique = true
	}
	if fields := eeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
	}
	if ps := eeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eeq *EffectiveEntitlementQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eeq.driver.Dialect())
	t1 := builder.Table(effectiveentitlement.Table)
	columns := eeq.ctx.Fields
	if len(columns) == 0 {
		columns = effectiveentitlement.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eeq.sql != nil {
		selector = eeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eeq.ctx.Unique != nil && *eeq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eeq.predicates {
		p(selector)
	}
	for _, p := range eeq.order {
		p(selector)
	}
	if offset := eeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EffectiveEntitlementGroupBy is the group-by builder for EffectiveEntitlement entities.
type EffectiveEntitlementGroupBy struct {
	selector
	build *EffectiveEntitlementQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eegb *EffectiveEntitlementGroupBy) Aggregate(fns ...AggregateFunc) *EffectiveEntitlementGroupBy {
	eegb.fns = append(eegb.fns, fns...)
	return eegb
}

// Scan applies the selector query and scans the result into the given value.
func (eegb *EffectiveEntitlementGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eegb.build.ctx, ent.OpQueryGroupBy)
	if err := eegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EffectiveEntitlementQuery, *EffectiveEntitlementGroupBy](ctx, eegb.build, eegb, eegb.build.inters, v)
}

func (eegb *EffectiveEntitlementGroupBy) sqlScan(ctx context.Context, root *EffectiveEntitlementQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eegb.fns))
	for _, fn := range eegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eegb.flds)+len(eegb.fns))
		for _, f := range *eegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EffectiveEntitlementSelect is the builder for selecting fields of EffectiveEntitlement entities.
type EffectiveEntitlementSelect struct {
	*EffectiveEntitlementQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ees *EffectiveEntitlementSelect) Aggregate(fns ...AggregateFunc) *EffectiveEntitlementSelect {
	ees.fns = append(ees.fns, fns...)
	return ees
}

// Scan applies the selector query and scans the result into the given value.
func (ees *EffectiveEntitlementSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ees.ctx, ent.OpQuerySelect)
	if err := ees.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EffectiveEntitlementQuery, *EffectiveEntitlementSelect](ctx, ees.EffectiveEntitlementQuery, ees, ees.inters, v)
}

func (ees *EffectiveEntitlementSelect) sqlScan(ctx context.Context, root *EffectiveEntitlementQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ees.fns))
	for _, fn := range ees.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ees.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ees.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
