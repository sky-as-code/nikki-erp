// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sky-as-code/nikki-erp/modules/authorize/infra/ent/predicate"
	"github.com/sky-as-code/nikki-erp/modules/authorize/infra/ent/rolesuite"
	"github.com/sky-as-code/nikki-erp/modules/authorize/infra/ent/rolesuiteuser"
)

// RoleSuiteUserQuery is the builder for querying RoleSuiteUser entities.
type RoleSuiteUserQuery struct {
	config
	ctx           *QueryContext
	order         []rolesuiteuser.OrderOption
	inters        []Interceptor
	predicates    []predicate.RoleSuiteUser
	withRoleSuite *RoleSuiteQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RoleSuiteUserQuery builder.
func (rsuq *RoleSuiteUserQuery) Where(ps ...predicate.RoleSuiteUser) *RoleSuiteUserQuery {
	rsuq.predicates = append(rsuq.predicates, ps...)
	return rsuq
}

// Limit the number of records to be returned by this query.
func (rsuq *RoleSuiteUserQuery) Limit(limit int) *RoleSuiteUserQuery {
	rsuq.ctx.Limit = &limit
	return rsuq
}

// Offset to start from.
func (rsuq *RoleSuiteUserQuery) Offset(offset int) *RoleSuiteUserQuery {
	rsuq.ctx.Offset = &offset
	return rsuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rsuq *RoleSuiteUserQuery) Unique(unique bool) *RoleSuiteUserQuery {
	rsuq.ctx.Unique = &unique
	return rsuq
}

// Order specifies how the records should be ordered.
func (rsuq *RoleSuiteUserQuery) Order(o ...rolesuiteuser.OrderOption) *RoleSuiteUserQuery {
	rsuq.order = append(rsuq.order, o...)
	return rsuq
}

// QueryRoleSuite chains the current query on the "role_suite" edge.
func (rsuq *RoleSuiteUserQuery) QueryRoleSuite() *RoleSuiteQuery {
	query := (&RoleSuiteClient{config: rsuq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rsuq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rsuq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rolesuiteuser.Table, rolesuiteuser.FieldID, selector),
			sqlgraph.To(rolesuite.Table, rolesuite.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, rolesuiteuser.RoleSuiteTable, rolesuiteuser.RoleSuiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(rsuq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RoleSuiteUser entity from the query.
// Returns a *NotFoundError when no RoleSuiteUser was found.
func (rsuq *RoleSuiteUserQuery) First(ctx context.Context) (*RoleSuiteUser, error) {
	nodes, err := rsuq.Limit(1).All(setContextOp(ctx, rsuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rolesuiteuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) FirstX(ctx context.Context) *RoleSuiteUser {
	node, err := rsuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RoleSuiteUser ID from the query.
// Returns a *NotFoundError when no RoleSuiteUser ID was found.
func (rsuq *RoleSuiteUserQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rsuq.Limit(1).IDs(setContextOp(ctx, rsuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rolesuiteuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) FirstIDX(ctx context.Context) int {
	id, err := rsuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RoleSuiteUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RoleSuiteUser entity is found.
// Returns a *NotFoundError when no RoleSuiteUser entities are found.
func (rsuq *RoleSuiteUserQuery) Only(ctx context.Context) (*RoleSuiteUser, error) {
	nodes, err := rsuq.Limit(2).All(setContextOp(ctx, rsuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rolesuiteuser.Label}
	default:
		return nil, &NotSingularError{rolesuiteuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) OnlyX(ctx context.Context) *RoleSuiteUser {
	node, err := rsuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RoleSuiteUser ID in the query.
// Returns a *NotSingularError when more than one RoleSuiteUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (rsuq *RoleSuiteUserQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rsuq.Limit(2).IDs(setContextOp(ctx, rsuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rolesuiteuser.Label}
	default:
		err = &NotSingularError{rolesuiteuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) OnlyIDX(ctx context.Context) int {
	id, err := rsuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RoleSuiteUsers.
func (rsuq *RoleSuiteUserQuery) All(ctx context.Context) ([]*RoleSuiteUser, error) {
	ctx = setContextOp(ctx, rsuq.ctx, ent.OpQueryAll)
	if err := rsuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RoleSuiteUser, *RoleSuiteUserQuery]()
	return withInterceptors[[]*RoleSuiteUser](ctx, rsuq, qr, rsuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) AllX(ctx context.Context) []*RoleSuiteUser {
	nodes, err := rsuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RoleSuiteUser IDs.
func (rsuq *RoleSuiteUserQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rsuq.ctx.Unique == nil && rsuq.path != nil {
		rsuq.Unique(true)
	}
	ctx = setContextOp(ctx, rsuq.ctx, ent.OpQueryIDs)
	if err = rsuq.Select(rolesuiteuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) IDsX(ctx context.Context) []int {
	ids, err := rsuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rsuq *RoleSuiteUserQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rsuq.ctx, ent.OpQueryCount)
	if err := rsuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rsuq, querierCount[*RoleSuiteUserQuery](), rsuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) CountX(ctx context.Context) int {
	count, err := rsuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rsuq *RoleSuiteUserQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rsuq.ctx, ent.OpQueryExist)
	switch _, err := rsuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rsuq *RoleSuiteUserQuery) ExistX(ctx context.Context) bool {
	exist, err := rsuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RoleSuiteUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rsuq *RoleSuiteUserQuery) Clone() *RoleSuiteUserQuery {
	if rsuq == nil {
		return nil
	}
	return &RoleSuiteUserQuery{
		config:        rsuq.config,
		ctx:           rsuq.ctx.Clone(),
		order:         append([]rolesuiteuser.OrderOption{}, rsuq.order...),
		inters:        append([]Interceptor{}, rsuq.inters...),
		predicates:    append([]predicate.RoleSuiteUser{}, rsuq.predicates...),
		withRoleSuite: rsuq.withRoleSuite.Clone(),
		// clone intermediate query.
		sql:  rsuq.sql.Clone(),
		path: rsuq.path,
	}
}

// WithRoleSuite tells the query-builder to eager-load the nodes that are connected to
// the "role_suite" edge. The optional arguments are used to configure the query builder of the edge.
func (rsuq *RoleSuiteUserQuery) WithRoleSuite(opts ...func(*RoleSuiteQuery)) *RoleSuiteUserQuery {
	query := (&RoleSuiteClient{config: rsuq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rsuq.withRoleSuite = query
	return rsuq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ApproverID string `json:"approver_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RoleSuiteUser.Query().
//		GroupBy(rolesuiteuser.FieldApproverID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rsuq *RoleSuiteUserQuery) GroupBy(field string, fields ...string) *RoleSuiteUserGroupBy {
	rsuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RoleSuiteUserGroupBy{build: rsuq}
	grbuild.flds = &rsuq.ctx.Fields
	grbuild.label = rolesuiteuser.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ApproverID string `json:"approver_id,omitempty"`
//	}
//
//	client.RoleSuiteUser.Query().
//		Select(rolesuiteuser.FieldApproverID).
//		Scan(ctx, &v)
func (rsuq *RoleSuiteUserQuery) Select(fields ...string) *RoleSuiteUserSelect {
	rsuq.ctx.Fields = append(rsuq.ctx.Fields, fields...)
	sbuild := &RoleSuiteUserSelect{RoleSuiteUserQuery: rsuq}
	sbuild.label = rolesuiteuser.Label
	sbuild.flds, sbuild.scan = &rsuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RoleSuiteUserSelect configured with the given aggregations.
func (rsuq *RoleSuiteUserQuery) Aggregate(fns ...AggregateFunc) *RoleSuiteUserSelect {
	return rsuq.Select().Aggregate(fns...)
}

func (rsuq *RoleSuiteUserQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rsuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rsuq); err != nil {
				return err
			}
		}
	}
	for _, f := range rsuq.ctx.Fields {
		if !rolesuiteuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rsuq.path != nil {
		prev, err := rsuq.path(ctx)
		if err != nil {
			return err
		}
		rsuq.sql = prev
	}
	return nil
}

func (rsuq *RoleSuiteUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RoleSuiteUser, error) {
	var (
		nodes       = []*RoleSuiteUser{}
		_spec       = rsuq.querySpec()
		loadedTypes = [1]bool{
			rsuq.withRoleSuite != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RoleSuiteUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RoleSuiteUser{config: rsuq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rsuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rsuq.withRoleSuite; query != nil {
		if err := rsuq.loadRoleSuite(ctx, query, nodes, nil,
			func(n *RoleSuiteUser, e *RoleSuite) { n.Edges.RoleSuite = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rsuq *RoleSuiteUserQuery) loadRoleSuite(ctx context.Context, query *RoleSuiteQuery, nodes []*RoleSuiteUser, init func(*RoleSuiteUser), assign func(*RoleSuiteUser, *RoleSuite)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*RoleSuiteUser)
	for i := range nodes {
		fk := nodes[i].RoleSuiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rolesuite.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "role_suite_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rsuq *RoleSuiteUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rsuq.querySpec()
	_spec.Node.Columns = rsuq.ctx.Fields
	if len(rsuq.ctx.Fields) > 0 {
		_spec.Unique = rsuq.ctx.Unique != nil && *rsuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rsuq.driver, _spec)
}

func (rsuq *RoleSuiteUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rolesuiteuser.Table, rolesuiteuser.Columns, sqlgraph.NewFieldSpec(rolesuiteuser.FieldID, field.TypeInt))
	_spec.From = rsuq.sql
	if unique := rsuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rsuq.path != nil {
		_spec.Unique = true
	}
	if fields := rsuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rolesuiteuser.FieldID)
		for i := range fields {
			if fields[i] != rolesuiteuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rsuq.withRoleSuite != nil {
			_spec.Node.AddColumnOnce(rolesuiteuser.FieldRoleSuiteID)
		}
	}
	if ps := rsuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rsuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rsuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rsuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rsuq *RoleSuiteUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rsuq.driver.Dialect())
	t1 := builder.Table(rolesuiteuser.Table)
	columns := rsuq.ctx.Fields
	if len(columns) == 0 {
		columns = rolesuiteuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rsuq.sql != nil {
		selector = rsuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rsuq.ctx.Unique != nil && *rsuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rsuq.predicates {
		p(selector)
	}
	for _, p := range rsuq.order {
		p(selector)
	}
	if offset := rsuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rsuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RoleSuiteUserGroupBy is the group-by builder for RoleSuiteUser entities.
type RoleSuiteUserGroupBy struct {
	selector
	build *RoleSuiteUserQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rsugb *RoleSuiteUserGroupBy) Aggregate(fns ...AggregateFunc) *RoleSuiteUserGroupBy {
	rsugb.fns = append(rsugb.fns, fns...)
	return rsugb
}

// Scan applies the selector query and scans the result into the given value.
func (rsugb *RoleSuiteUserGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rsugb.build.ctx, ent.OpQueryGroupBy)
	if err := rsugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RoleSuiteUserQuery, *RoleSuiteUserGroupBy](ctx, rsugb.build, rsugb, rsugb.build.inters, v)
}

func (rsugb *RoleSuiteUserGroupBy) sqlScan(ctx context.Context, root *RoleSuiteUserQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rsugb.fns))
	for _, fn := range rsugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rsugb.flds)+len(rsugb.fns))
		for _, f := range *rsugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rsugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rsugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RoleSuiteUserSelect is the builder for selecting fields of RoleSuiteUser entities.
type RoleSuiteUserSelect struct {
	*RoleSuiteUserQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rsus *RoleSuiteUserSelect) Aggregate(fns ...AggregateFunc) *RoleSuiteUserSelect {
	rsus.fns = append(rsus.fns, fns...)
	return rsus
}

// Scan applies the selector query and scans the result into the given value.
func (rsus *RoleSuiteUserSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rsus.ctx, ent.OpQuerySelect)
	if err := rsus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RoleSuiteUserQuery, *RoleSuiteUserSelect](ctx, rsus.RoleSuiteUserQuery, rsus, rsus.inters, v)
}

func (rsus *RoleSuiteUserSelect) sqlScan(ctx context.Context, root *RoleSuiteUserQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rsus.fns))
	for _, fn := range rsus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rsus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rsus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
