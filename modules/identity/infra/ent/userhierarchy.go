// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/identity/infra/ent/hierarchylevel"
	"github.com/sky-as-code/nikki-erp/modules/identity/infra/ent/user"
	"github.com/sky-as-code/nikki-erp/modules/identity/infra/ent/userhierarchy"
)

// UserHierarchy is the model entity for the UserHierarchy schema.
type UserHierarchy struct {
	config `json:"-"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// HierarchyID holds the value of the "hierarchy_id" field.
	HierarchyID string `json:"hierarchy_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserHierarchyQuery when eager-loading is set.
	Edges        UserHierarchyEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserHierarchyEdges holds the relations/edges for other nodes in the graph.
type UserHierarchyEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Hierarchy holds the value of the hierarchy edge.
	Hierarchy *HierarchyLevel `json:"hierarchy,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserHierarchyEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// HierarchyOrErr returns the Hierarchy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserHierarchyEdges) HierarchyOrErr() (*HierarchyLevel, error) {
	if e.Hierarchy != nil {
		return e.Hierarchy, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: hierarchylevel.Label}
	}
	return nil, &NotLoadedError{edge: "hierarchy"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserHierarchy) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userhierarchy.FieldUserID, userhierarchy.FieldHierarchyID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserHierarchy fields.
func (uh *UserHierarchy) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userhierarchy.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				uh.UserID = value.String
			}
		case userhierarchy.FieldHierarchyID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hierarchy_id", values[i])
			} else if value.Valid {
				uh.HierarchyID = value.String
			}
		default:
			uh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserHierarchy.
// This includes values selected through modifiers, order, etc.
func (uh *UserHierarchy) Value(name string) (ent.Value, error) {
	return uh.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserHierarchy entity.
func (uh *UserHierarchy) QueryUser() *UserQuery {
	return NewUserHierarchyClient(uh.config).QueryUser(uh)
}

// QueryHierarchy queries the "hierarchy" edge of the UserHierarchy entity.
func (uh *UserHierarchy) QueryHierarchy() *HierarchyLevelQuery {
	return NewUserHierarchyClient(uh.config).QueryHierarchy(uh)
}

// Update returns a builder for updating this UserHierarchy.
// Note that you need to call UserHierarchy.Unwrap() before calling this method if this UserHierarchy
// was returned from a transaction, and the transaction was committed or rolled back.
func (uh *UserHierarchy) Update() *UserHierarchyUpdateOne {
	return NewUserHierarchyClient(uh.config).UpdateOne(uh)
}

// Unwrap unwraps the UserHierarchy entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uh *UserHierarchy) Unwrap() *UserHierarchy {
	_tx, ok := uh.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserHierarchy is not a transactional entity")
	}
	uh.config.driver = _tx.drv
	return uh
}

// String implements the fmt.Stringer.
func (uh *UserHierarchy) String() string {
	var builder strings.Builder
	builder.WriteString("UserHierarchy(")
	builder.WriteString("user_id=")
	builder.WriteString(uh.UserID)
	builder.WriteString(", ")
	builder.WriteString("hierarchy_id=")
	builder.WriteString(uh.HierarchyID)
	builder.WriteByte(')')
	return builder.String()
}

// UserHierarchies is a parsable slice of UserHierarchy.
type UserHierarchies []*UserHierarchy
