// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky-as-code/nikki-erp/modules/identity/infra/ent/userstatusenum"
)

// UserStatusEnum is the model entity for the UserStatusEnum schema.
type UserStatusEnum struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Etag holds the value of the "etag" field.
	Etag string `json:"etag,omitempty"`
	// Label holds the value of the "label" field.
	Label map[string]string `json:"label,omitempty"`
	// Value holds the value of the "value" field.
	Value string `json:"value,omitempty"`
	// Type holds the value of the "type" field.
	Type string `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserStatusEnumQuery when eager-loading is set.
	Edges        UserStatusEnumEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserStatusEnumEdges holds the relations/edges for other nodes in the graph.
type UserStatusEnumEdges struct {
	// Users holds the value of the users edge.
	Users []*User `json:"users,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UsersOrErr returns the Users value or an error if the edge
// was not loaded in eager-loading.
func (e UserStatusEnumEdges) UsersOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.Users, nil
	}
	return nil, &NotLoadedError{edge: "users"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserStatusEnum) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userstatusenum.FieldLabel:
			values[i] = new([]byte)
		case userstatusenum.FieldID, userstatusenum.FieldEtag, userstatusenum.FieldValue, userstatusenum.FieldType:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserStatusEnum fields.
func (use *UserStatusEnum) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userstatusenum.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				use.ID = value.String
			}
		case userstatusenum.FieldEtag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field etag", values[i])
			} else if value.Valid {
				use.Etag = value.String
			}
		case userstatusenum.FieldLabel:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &use.Label); err != nil {
					return fmt.Errorf("unmarshal field label: %w", err)
				}
			}
		case userstatusenum.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				use.Value = value.String
			}
		case userstatusenum.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				use.Type = value.String
			}
		default:
			use.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the UserStatusEnum.
// This includes values selected through modifiers, order, etc.
func (use *UserStatusEnum) GetValue(name string) (ent.Value, error) {
	return use.selectValues.Get(name)
}

// QueryUsers queries the "users" edge of the UserStatusEnum entity.
func (use *UserStatusEnum) QueryUsers() *UserQuery {
	return NewUserStatusEnumClient(use.config).QueryUsers(use)
}

// Update returns a builder for updating this UserStatusEnum.
// Note that you need to call UserStatusEnum.Unwrap() before calling this method if this UserStatusEnum
// was returned from a transaction, and the transaction was committed or rolled back.
func (use *UserStatusEnum) Update() *UserStatusEnumUpdateOne {
	return NewUserStatusEnumClient(use.config).UpdateOne(use)
}

// Unwrap unwraps the UserStatusEnum entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (use *UserStatusEnum) Unwrap() *UserStatusEnum {
	_tx, ok := use.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserStatusEnum is not a transactional entity")
	}
	use.config.driver = _tx.drv
	return use
}

// String implements the fmt.Stringer.
func (use *UserStatusEnum) String() string {
	var builder strings.Builder
	builder.WriteString("UserStatusEnum(")
	builder.WriteString(fmt.Sprintf("id=%v, ", use.ID))
	builder.WriteString("etag=")
	builder.WriteString(use.Etag)
	builder.WriteString(", ")
	builder.WriteString("label=")
	builder.WriteString(fmt.Sprintf("%v", use.Label))
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(use.Value)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(use.Type)
	builder.WriteByte(')')
	return builder.String()
}

// UserStatusEnums is a parsable slice of UserStatusEnum.
type UserStatusEnums []*UserStatusEnum
